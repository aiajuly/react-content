// remember that components are just functions

// when you render the component you can provide arguments with it as following: <component person="bill"/>
// in your component function you will have an object parameter that will collect all the arguments: you can call that object props so props.person
// typically in the function definetion we destructure the arguments({person})

// for arguments that are not strings( numbers, bools, objects, arrays); use curely braces <component number={20}/> or <component numbers={[1,2,3]}/>


